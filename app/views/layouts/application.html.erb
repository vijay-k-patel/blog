<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag "turbo", type: "module" %>
  </head>

  <body class='prose mx-auto'>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <div class='flex mb-4 justify-between items-center'>
      <div class='text-2xl font-black'>My Blog</div>
      <div class='flex'>
        <% if user_signed_in? %>
          <%= link_to 'New blog post', new_blog_post_path, class: 'bg-blue-500 hover:bg-blue-700 mr-4 text-white font-bold py-2 px-4 rounded no-underline'%>
          <%= button_to 'Logout', destroy_user_session_path, method: :delete, class: 'bg-gray-300 hover:bg-gray-400 text-gray-800 font-bold py-2 px-4 rounded' %>
        <% end %>
      </div>
    </div>

    <%= yield %>
  </body>
</html>
